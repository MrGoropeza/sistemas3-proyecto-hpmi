import { NombreDepositoPipe } from './nombre-deposito.pipe';

describe('NombreDepositoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreDepositoPipe();
    expect(pipe).toBeTruthy();
  });
});
